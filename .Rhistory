pacman::p_load(seminr, rio)
dataku <- import("df_sem.xlsx")
dataku <- na.omit(dataku)
head(df)
# -------------------------------------------------------------------
# LANGKAH 1: MEMBUAT MODEL PENGUKURAN (MEASUREMENT MODEL)
# -------------------------------------------------------------------
m_model <- constructs(
composite("A", multi_items("A_", 1:8), weights = mode_B), # formatif
composite("C", multi_items("C_", 1:3), weights = mode_B), # formatif
composite("D", multi_items("D_", 1:2), weights = mode_A), # reflektif
composite("F", multi_items("F_", 1:3), weights = mode_A), # reflektif
composite("G", multi_items("G_", 1:3), weights = mode_A), # reflektif
composite("I", single_item("I"), weights = mode_A)
)
# -------------------------------------------------------------------
# LANGKAH 2: MEMBUAT MODEL STRUKTURAL (STRUCTURAL MODEL)
# -------------------------------------------------------------------
s_model <- relationships(
paths(from = c("F", "G", "C"), to = c("A")),
paths(from = c("A"), to = c("D")),
paths(from = c("D", "G"), to = "I")
)
# -------------------------------------------------------------------
# LANGKAH 3: MENJALANKAN ANALISIS PLS-SEM
# -------------------------------------------------------------------
pls_model <- estimate_pls(
data = dataku,
measurement_model = m_model,
structural_model = s_model,
inner_weights = path_weighting,
missing = mean_replacement,
missing_value = "-99"
)
# Lihat ringkasan hasil awal
summary_model <- summary(pls_model)
plot(pls_model)
plot(pls_model)
dataku <- import("df_sem.xlsx")
dataku <- na.omit(dataku)
head(df)
# -------------------------------------------------------------------
# LANGKAH 1: MEMBUAT MODEL PENGUKURAN (MEASUREMENT MODEL)
# -------------------------------------------------------------------
m_model <- constructs(
composite("strategi", multi_items("A_", 1:8), weights = mode_B), # formatif
composite("institusional", multi_items("C_", 1:3), weights = mode_B), # formatif
composite("kualitas", multi_items("D_", 1:2), weights = mode_A), # reflektif
composite("konten", multi_items("F_", 1:3), weights = mode_A), # reflektif
composite("pedagogis", multi_items("G_", 1:3), weights = mode_A), # reflektif
composite("pemahaman", single_item("I"), weights = mode_A)
)
# -------------------------------------------------------------------
# LANGKAH 2: MEMBUAT MODEL STRUKTURAL (STRUCTURAL MODEL)
# -------------------------------------------------------------------
s_model <- relationships(
paths(from = c("konten", "pedagogis", "institusional"), to = c("strategi")),
paths(from = c("strategi"), to = c("kualitas")),
paths(from = c("kualitas", "pedagogis"), to = "pemahaman")
)
# -------------------------------------------------------------------
# LANGKAH 3: MENJALANKAN ANALISIS PLS-SEM
# -------------------------------------------------------------------
pls_model <- estimate_pls(
data = dataku,
measurement_model = m_model,
structural_model = s_model,
inner_weights = path_weighting,
missing = mean_replacement,
missing_value = "-99"
)
# Lihat ringkasan hasil awal
summary_model <- summary(pls_model)
plot(pls_model)
paste("The algorithm converged after iteration" ,summary_model$iterations)
# Inspect the outer loadings
summary_model$loadings
# Inspect the indicator loadings
summary_model$loadings^2
# Inspect the composite reliability
summary_model$reliability
plot(summary_model$reliability)
# Table of the FL Criteria
summary_model$validity$fl_criteria
# HTMT criterion
summary_model$validity$htmt
boot_rep <- bootstrap_model(seminr_model = pls_model, nboot = 5000)
sum_boot_rep <- summary(boot_rep, alpha=0.10)
# Extract the bootstrapped HTMT
sum_boot_rep$bootstrapped_HTMT
dataku <- import("df_sem.xlsx")
dataku <- na.omit(dataku)
head(df)
# -------------------------------------------------------------------
# LANGKAH 1: MEMBUAT MODEL PENGUKURAN (MEASUREMENT MODEL)
# -------------------------------------------------------------------
m_model <- constructs(
composite("strategi", multi_items("A_", 1:8), weights = mode_B), # formatif
composite("institusional", multi_items("C_", 1:3), weights = mode_B), # formatif
composite("kualitas", multi_items("D_", 1:2), weights = mode_A), # reflektif
composite("konten", multi_items("F_", 1:3), weights = mode_A), # reflektif
composite("pedagogis", multi_items("G_", 1:3), weights = mode_A), # reflektif
composite("pemahaman", single_item("I"), weights = mode_A)
)
# -------------------------------------------------------------------
# LANGKAH 2: MEMBUAT MODEL STRUKTURAL (STRUCTURAL MODEL)
# -------------------------------------------------------------------
s_model <- relationships(
paths(from = c("konten", "pedagogis"), to = c("kompetensi")),
paths(from = c("kompetensi", "institusional"), to = c("strategi")),
paths(from = c("strategi"), to = c("kualitas")),
paths(from = c("kualitas", "pedagogis"), to = "pemahaman")
)
# -------------------------------------------------------------------
# LANGKAH 3: MENJALANKAN ANALISIS PLS-SEM
# -------------------------------------------------------------------
pls_model <- estimate_pls(
data = dataku,
measurement_model = m_model,
structural_model = s_model,
inner_weights = path_weighting,
missing = mean_replacement,
missing_value = "-99"
)
# -------------------------------------------------------------------
# LANGKAH 2: MEMBUAT MODEL STRUKTURAL (STRUCTURAL MODEL)
# -------------------------------------------------------------------
s_model <- relationships(
paths(from = c("konten", "pedagogis"), to = "kompetensi"),
paths(from = c("kompetensi", "institusional"), to = c("strategi")),
paths(from = c("strategi"), to = c("kualitas")),
paths(from = c("kualitas", "pedagogis"), to = "pemahaman")
)
# -------------------------------------------------------------------
# LANGKAH 3: MENJALANKAN ANALISIS PLS-SEM
# -------------------------------------------------------------------
pls_model <- estimate_pls(
data = dataku,
measurement_model = m_model,
structural_model = s_model,
inner_weights = path_weighting,
missing = mean_replacement,
missing_value = "-99"
)
# -------------------------------------------------------------------
# LANGKAH 2: MEMBUAT MODEL STRUKTURAL (STRUCTURAL MODEL)
# -------------------------------------------------------------------
s_model <- relationships(
paths(from = c("konten", "pedagogis"), to = "kompetensi"),
paths(from = c("kompetensi", "institusional"), to = "strategi"),
paths(from = c("strategi"), to = c("kualitas")),
paths(from = c("kualitas", "pedagogis"), to = "pemahaman")
)
# -------------------------------------------------------------------
# LANGKAH 3: MENJALANKAN ANALISIS PLS-SEM
# -------------------------------------------------------------------
pls_model <- estimate_pls(
data = dataku,
measurement_model = m_model,
structural_model = s_model,
inner_weights = path_weighting,
missing = mean_replacement,
missing_value = "-99"
)
# -------------------------------------------------------------------
# LANGKAH 2: MEMBUAT MODEL STRUKTURAL (STRUCTURAL MODEL)
# -------------------------------------------------------------------
#s_model <- relationships(
#  paths(from = c("konten", "pedagogis"), to = "kompetensi"),
#  paths(from = c("kompetensi", "institusional"), to = "strategi"),
#  paths(from = c("strategi"), to = c("kualitas")),
#  paths(from = c("kualitas", "pedagogis"), to = "pemahaman")
#)
s_model <- relationships(
paths(from = c("konten", "pedagogis", "institusional"), to = c("strategi")),
paths(from = c("strategi"), to = c("kualitas")),
paths(from = c("kualitas", "pedagogis"), to = "pemahaman")
)
# -------------------------------------------------------------------
# LANGKAH 3: MENJALANKAN ANALISIS PLS-SEM
# -------------------------------------------------------------------
pls_model <- estimate_pls(
data = dataku,
measurement_model = m_model,
structural_model = s_model,
inner_weights = path_weighting,
missing = mean_replacement,
missing_value = "-99"
)
# Lihat ringkasan hasil awal
summary_model <- summary(pls_model)
# -------------------------------------------------------------------
# LANGKAH 2: MEMBUAT MODEL STRUKTURAL (STRUCTURAL MODEL)
# -------------------------------------------------------------------
s_model <- relationships(
paths(from = c("konten", "pedagogis"), to = c("kompetensi")),
paths(from = c("kompetensi", "institusional"), to = c("strategi")),
paths(from = c("strategi"), to = c("kualitas")),
paths(from = c("kualitas", "pedagogis"), to = "pemahaman")
)
# -------------------------------------------------------------------
# LANGKAH 3: MENJALANKAN ANALISIS PLS-SEM
# -------------------------------------------------------------------
pls_model <- estimate_pls(
data = dataku,
measurement_model = m_model,
structural_model = s_model,
inner_weights = path_weighting,
missing = mean_replacement,
missing_value = "-99"
)
# -------------------------------------------------------------------
# LANGKAH 2: MEMBUAT MODEL STRUKTURAL (STRUCTURAL MODEL)
# -------------------------------------------------------------------
s_model_baru <- relationships(
# 1. MEMBENTUK KONSTRUK ORDE KEDUA (FORMATIF)
# 'Kompetensi_Mengajar' dibentuk oleh 'konten' dan 'pedagogis'
paths(from = c("konten", "pedagogis"), to = "Kompetensi_Mengajar"),
# 2. MENGHUBUNGKAN KONSTRUK ORDE KEDUA KE MODEL
# Sekarang 'Kompetensi_Mengajar' yang memengaruhi 'strategi'
paths(from = c("Kompetensi_Mengajar", "institusional"), to = c("strategi")),
# 3. JALUR LAINNYA TETAP SAMA
paths(from = c("strategi"), to = c("kualitas")),
paths(from = c("kualitas", "pedagogis"), to = "pemahaman")
)
# -------------------------------------------------------------------
# LANGKAH 3: MENJALANKAN ANALISIS PLS-SEM
# -------------------------------------------------------------------
pls_model <- estimate_pls(
data = dataku,
measurement_model = m_model,
structural_model = s_model,
inner_weights = path_weighting,
missing = mean_replacement,
missing_value = "-99"
)
# -------------------------------------------------------------------
# LANGKAH 2: MEMBUAT MODEL STRUKTURAL (STRUCTURAL MODEL)
# -------------------------------------------------------------------
s_model <- relationships(
paths(from = c("konten", "pedagogis"), to = c("kompetensi")),
paths(from = c("kompetensi", "institusional"), to = c("strategi")),
paths(from = c("strategi"), to = c("kualitas")),
paths(from = c("kualitas", "pedagogis"), to = "pemahaman")
)
# -------------------------------------------------------------------
# LANGKAH 3: MENJALANKAN ANALISIS PLS-SEM
# -------------------------------------------------------------------
pls_model <- estimate_pls(
data = dataku,
measurement_model = m_model,
structural_model = s_model,
inner_weights = path_weighting,
missing = mean_replacement,
missing_value = "-99"
)
# -------------------------------------------------------------------
# LANGKAH 1: MEMBUAT MODEL PENGUKURAN (MEASUREMENT MODEL)
# -------------------------------------------------------------------
m_model <- constructs(
composite("strategi", multi_items("A_", 1:8), weights = mode_B), # formatif
composite("institusional", multi_items("C_", 1:3), weights = mode_B), # formatif
composite("kualitas", multi_items("D_", 1:2), weights = mode_A), # reflektif
composite("konten", multi_items("F_", 1:3), weights = mode_A), # reflektif
composite("pedagogis", multi_items("G_", 1:3), weights = mode_A), # reflektif
composite("pemahaman", single_item("I"), weights = mode_A),
composite("kompetensi", c("konten", "pedagogis"), weights = mode_B)
)
# -------------------------------------------------------------------
# LANGKAH 2: MEMBUAT MODEL STRUKTURAL (STRUCTURAL MODEL)
# -------------------------------------------------------------------
s_model <- relationships(
paths(from = c("kompetensi", "institusional"), to = c("strategi")),
paths(from = c("strategi"), to = c("kualitas")),
paths(from = c("kualitas", "pedagogis"), to = "pemahaman")
)
# -------------------------------------------------------------------
# LANGKAH 3: MENJALANKAN ANALISIS PLS-SEM
# -------------------------------------------------------------------
pls_model <- estimate_pls(
data = dataku,
measurement_model = m_model,
structural_model = s_model,
inner_weights = path_weighting,
missing = mean_replacement,
missing_value = "-99"
)
